[defaults]
# The role's Galaxy name (inspectah) differs from the repo directory name
# (ansible-role-inspectah). Point roles_path at .roles/ where a tracked
# symlink maps the Galaxy name back to the repo root, so Ansible resolves
# role references in examples/, tests/, and smoke playbooks from a
# normal checkout.
roles_path = .roles
